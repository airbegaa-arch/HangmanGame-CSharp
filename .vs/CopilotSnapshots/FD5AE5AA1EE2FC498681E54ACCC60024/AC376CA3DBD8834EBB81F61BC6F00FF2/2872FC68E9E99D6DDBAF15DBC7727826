class Program
{
    static void Main()
    {
        string name = "ABCDEF";
        // strings are immutable in C#; create a modifiable char array and rebuild the string
        var chars = name.ToCharArray();
        chars[3] = 'B';
        name = new string(chars);
    }
}